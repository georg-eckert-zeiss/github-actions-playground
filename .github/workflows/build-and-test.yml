name: Build & Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    uses: georg-eckert-zeiss/github-actions/.github/workflows/build-and-pack.yml@docx
    with:
      configuration: "Release"
      do_pack: false
      is_prerelease: false
      suffix: "none"
      publish_target: "None"
    secrets: inherit
